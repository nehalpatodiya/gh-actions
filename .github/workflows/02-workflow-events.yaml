name: workflow-events
on:
    push:
    pull_request:
    schedule:
        - cron: "0/5 * * * *"

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: |
                echo "Event name: ${{ github.event_name }}"